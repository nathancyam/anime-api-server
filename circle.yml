machine:
  timezone: Australia/Melbourne
  node:
    version: 6.9.1

dependencies:
  pre:
    - echo "{}" > /tmp/config.json

test:
  override:
    - npm run unittest

deployment:
  production:
    branch: config_file
    commands:
      - ./deploy/prod.sh
