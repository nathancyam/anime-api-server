machine:
  timezone: Australia/Melbourne
  node:
    version: 6.9.1

dependencies:
  pre:
    - echo "{}" > /tmp/config.json

hosts:
  circlehost: 127.0.0.1

test:
  override:
    - npm run unittest

deployment:
  production:
    branch: config_file
    commands:
      - chmod +x ./deploy/prod.sh
      - ./deploy/prod.sh
